namespace SolidTraining.ISP
{
    /// <summary>
    /// The “I” in SOLID is for Interface Segregation Principle, 
    /// which states that clients should not be forced to depend on methods they don’t use. 
    /// If a class exposes so many members that those members can be broken down into groups 
    /// that serve different clients that don’t use members from the other groups, 
    /// you should think about exposing those member groups as separate interfaces.
    /// </summary>
    public class InterfaceSegregationPrinciple
    {
        public static void Run()
        {
            // Check OrderHandler
            // Check OnBoardingHandlerTests
        }
    }
}